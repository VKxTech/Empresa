<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VKX Admin | Login</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-dark flex items-center justify-center min-h-screen p-6">
    <div class="max-w-sm w-full bg-dark-surface border border-white/5 rounded-3xl p-10 shadow-2xl">
        <div class="text-center mb-10">
            <h1 class="text-3xl font-outfit font-black text-gradient">VKX ADMIN</h1>
            <p class="text-white/40 text-sm mt-2">Acesso restrito ao painel de gestão</p>
        </div>

        <% if (locals.error) { %>
            <div class="bg-red-500/10 border border-red-500/20 text-red-500 p-4 rounded-xl text-sm mb-6 text-center">
                <%= error %>
            </div>
        <% } %>

        <form action="/admin/login" method="POST" class="space-y-6">
            <div>
                <label class="block text-xs font-bold text-white/40 uppercase tracking-widest mb-2">E-mail</label>
                <input type="email" name="email" required class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 outline-none focus:border-primary/50 text-white">
            </div>
            <div>
                <label class="block text-xs font-bold text-white/40 uppercase tracking-widest mb-2">Senha</label>
                <input type="password" name="password" required class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 outline-none focus:border-primary/50 text-white">
            </div>
            <button type="submit" class="w-full btn-primary py-4">Entrar no Sistema</button>
        </form>
    </div>
</body>
</html>
